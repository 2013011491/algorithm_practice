#include<iostream>

using namespace std;

long long mLen[50000];
int main(void)
{
    cin.tie(NULL);
    ios::sync_with_stdio(false);

    int t,n;
    cin>>t;
    for(int tc=0; tc<t; tc++)
    {
        cin>>n;
        for(int i=0; i<n; i++) cin>>mLen[i];
        cout<<"#"<<tc+1<<" ";

        //start
        
    }
    return 0;
}